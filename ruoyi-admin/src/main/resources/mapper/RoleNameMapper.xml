<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.mapper.RoleNameMapper">

    <resultMap id="BaseResultMap" type="com.ruoyi.pojo.RoleName">
            <id property="roleId" column="role_id" jdbcType="INTEGER"/>
            <result property="role" column="role" jdbcType="VARCHAR"/>
    </resultMap>

    <sql id="Base_Column_List">
        role_id,role
    </sql>

    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from role_name
        where  role_id = #{roleId,jdbcType=INTEGER} 
    </select>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        delete from role_name
        where  role_id = #{roleId,jdbcType=INTEGER} 
    </delete>
    <insert id="insert" keyColumn="role_id" keyProperty="roleId" parameterType="com.ruoyi.pojo.RoleName" useGeneratedKeys="true">
        insert into role_name
        ( role_id,role)
        values (#{roleId,jdbcType=INTEGER},#{role,jdbcType=VARCHAR})
    </insert>
    <insert id="insertSelective" keyColumn="role_id" keyProperty="roleId" parameterType="com.ruoyi.pojo.RoleName" useGeneratedKeys="true">
        insert into role_name
        <trim prefix="(" suffix=")" suffixOverrides=",">
                <if test="roleId != null">role_id,</if>
                <if test="role != null">role,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
                <if test="roleId != null">#{roleId,jdbcType=INTEGER},</if>
                <if test="role != null">#{role,jdbcType=VARCHAR},</if>
        </trim>
    </insert>
    <update id="updateByPrimaryKeySelective" parameterType="com.ruoyi.pojo.RoleName">
        update role_name
        <set>
                <if test="role != null">
                    role = #{role,jdbcType=VARCHAR},
                </if>
        </set>
        where   role_id = #{roleId,jdbcType=INTEGER} 
    </update>
    <update id="updateByPrimaryKey" parameterType="com.ruoyi.pojo.RoleName">
        update role_name
        set 
            role =  #{role,jdbcType=VARCHAR}
        where   role_id = #{roleId,jdbcType=INTEGER} 
    </update>
</mapper>
